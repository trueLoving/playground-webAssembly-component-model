// import fs from "node:fs";

export function add(x, y) {
  return x + y;
}

// ???todo: how to combine with node native module
// export function readfile(filename) {
//   return fs.readFileSync(filename, { encoding: "utf-8" });
// }
